package com.firestorm.newview

// LLAgent.kt was the original stub; the full implementation lives in Agent.kt.
// This file is retained only as a redirect so existing call sites still compile.

// No new declarations here — Agent.kt provides the Agent class and gAgent accessor.
