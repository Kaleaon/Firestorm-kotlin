package com.firestorm.newview

// llfloaterwindlight.h and llfloaterwindlight.cpp do not exist in the Firestorm
// source tree at /tmp/phoenix-firestorm/indra/newview/. WindLight environment
// editing was migrated to the EEP (Environmental Enhancement Project) system and
// its floaters live in llfloatereditenvironment.h/.cpp and related files.
// This file is intentionally left as a namespace-only placeholder so that any
// existing call sites referencing LLFloaterWindLightUtil compile without error.

object LLFloaterWindLightUtil {
    fun registerFloater() {
        TODO("Source files llfloaterwindlight.h/.cpp are absent — WindLight UI was superseded by EEP; wire up the EEP floater here instead")
    }
}
