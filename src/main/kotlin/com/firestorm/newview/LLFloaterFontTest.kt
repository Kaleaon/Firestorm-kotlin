package com.firestorm.newview

/**
 * Floater used to exercise / visually inspect the standard font set.
 * The C++ class body is intentionally minimal (constructor + destructor only);
 * the actual panel content is driven by the XML layout "floater_font_test".
 */
class LLFloaterFontTest(key: LLSD) : LLFloater("floater_font_test") {

    init {
        TODO("APR: use JVM equivalent – build UI from floater_font_test XML layout")
    }
}
